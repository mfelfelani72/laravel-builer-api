services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: laravel_builer_api
    volumes:
      - ./src:/var/www/html
    networks:
      - laravel_builer_api_network

  webserver:
    image: nginx:alpine
    container_name: laravel_builer_api_webserver
    ports:
      - "8000:8000"
    volumes:
      - ./src:/var/www/html
      - ./dockerFiles/nginx/app.conf:/etc/nginx/conf.d/app.conf
    depends_on:
      - app
    networks:
      - laravel_builer_api_network

  db:
    image: mysql:5.7
    container_name: laravel_builer_api_db
    environment:
      MYSQL_ROOT_HOST: '%'
      MYSQL_DATABASE: laravel-builer-api
      MYSQL_USER: mohammadfe
      MYSQL_PASSWORD: "12345"
      MYSQL_ROOT_PASSWORD: secret
    ports:
      - "3307:3306"
    volumes:
      - dbdata:/var/lib/mysql
      - ./dockerFiles/sql/init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - laravel_builer_api_network
    command: [
        "mysqld",
        "--init-file=/docker-entrypoint-initdb.d/init.sql"
    ]

volumes:
  dbdata:

networks:
  laravel_builer_api_network:
    driver: bridge
